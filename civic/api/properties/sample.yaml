server:
    port: port.number

db:
    main:
        host: sample.host
        port: sample.port
        username: sample.username
        password: sample.password
        database: sample.database
        pool: sample.pool.size